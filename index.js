const fs = require("fs");
const path = rerquire("path");
const inquirer = require("inquirer");
const generateReadme = require("");

const questions = [];

function writeFile() {}
function init() {}

init();
